{
	"manifest_version": 2,

	"name": "Quote It",

	"version": "1.0.3",

	"default_locale": "en",

	"browser_specific_settings": {
		"gecko": {
			"strict_min_version": "57.0"
		}
	},

	"description": "Save quotes from across the web.",

	"icons": {
		"48": "icons/quotes-48.png"
	},

	"background": {
		"scripts": ["background.js"]
	},

	"permissions": ["menus", "activeTab", "storage", "tabs", "find"],

	"optional_permissions": ["<all_urls>", "notifications"],

	"page_action": {
		"browser_style": false,
		"default_icon": "icons/quotes-48.png",
		"default_title": "Whereami?",
		"default_popup": "pageAction/editQuote.jsx",
		"show_matches": ["<all_urls>"]
	},

	"sidebar_action": {
		"default_icon": "icons/quotes-48.png",
		"default_title": "Quotes",
		"default_panel": "sidebar/panel.jsx"
	},

	"commands": {
		"_execute_sidebar_action": {
			"suggested_key": {
				"default": "Ctrl+Q"
			}
		}
	}
}
